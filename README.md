# Eninja

## 初始化

```bash
git clone https://github.com/Etuloser/eninja.git
cd eninja
python3 -m venv venv
source venv/bin/activate
python3 -m pip install --ugprade pip
pip3 install -r requirements.txt
python3 manage.py migrate
python3 manage.py runserver 0.0.0.0:8000
```
